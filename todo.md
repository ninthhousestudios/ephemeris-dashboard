92 → 85

ayanamsha tab:
on mobile, user-defined ayanamsha the box labels are on the boxes; they get cut off
for both desktop and mobile, change to labe: box, labe: box, etc.

adjust the user-defined ayanamsha:
    if a user makes one in the context bar, have it appear in the ayanamasha tab;
    include a note that this ayanamsha can be deleted or modified in the ayanamsha tab
    include an optional name entry for each user-defined ayanamsha; without it,
user-defined 1, 2, etc.
    any user-defined ayanamshas in the ayanamsha tab appear in the context bar drop down
    thus, a user can define multiple ayanamsha in the ayanamsha tab or through the
context bar - i.e., the ayanamsha tab and context bar user-defined ayanamshas are the
same and read from the same places


series:
    vertical vs. hortizonal -- this doesnt do anything

heliacal:
    when changing Scale in the context bar the Julian Days stay UT
